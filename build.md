Phased Roadmap for AI-Assisted Implementation
To build this product quickly using AI assistants, prompts should be executed sequentially, building upon previous code context:

Phase 1 (Database): Initialize Supabase database tables and create an images storage bucket with public read permissions.

Phase 2 (Form Construction): Prompt your AI tool to build the multi-step frontend wizard component utilizing the conditional property fields.

Phase 3 (Feed Engineering): Generate the /jobs page layout optimized for mobile screen configurations, displaying spatial context via UI badges.

Phase 4 (Deep Links Integration): Implement the bid submission routing and connect HTML link triggers for native mobile actions (tel: and viber://).